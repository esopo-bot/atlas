# painel

Dispara sessão headless e mostra o estado do executor de roteiros, no
navegador. Servidor
de biblioteca padrão, sem dependência: `python .agents/painel/painel.py`.

Depende do módulo `encadeador` — o painel de controle é vidro, o motor é ele.

Instalar: `python montar.py --modulo painel`
