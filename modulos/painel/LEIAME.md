# painel

Dispara sessão headless e mostra o estado da esteira, no navegador. Servidor
de biblioteca padrão, sem dependência: `python .agents/painel/painel.py`.

Depende do módulo `encadeador` — o painel é vidro, o motor é ele.

Instalar: `python montar.py --modulo painel`
